# Dependencies
node_modules/

# Build output
dist/

# Environment variables
.env

# Cloudflare local-dev secrets (keep .dev.vars.example, ignore real .dev.vars)
.dev.vars

# Private, non-published reference docs (WP plugin source, runbook with feed details)
_private/

# macOS
.DS_Store

# Local Claude Code tooling
.claude/

# Generated theme package (built to ../ziller-theme.zip; never committed)
*.zip

# Local build staging (normally the system temp dir; ignore if ever created here)
theme-build/
.build/
